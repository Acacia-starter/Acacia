
doctype html
html(lang="fr")

  include ./commons/head

  body(class=pageName)
    include ./commons/header
    main(router-wrapper)
      div(router-view=pageName)
        block content
    include ./commons/footer
