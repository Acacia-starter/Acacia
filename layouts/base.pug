- bodyClasses = bodyClasses || []
- bodyClasses.push(pageName)

block var

doctype html
html(lang="fr")

  include /components/head/index

  body(class=bodyClasses.join(' '), data-page-id=pageId)

    //- h1= htmlWebpackPlugin.files

    //- if htmlWebpackPlugin.files.sprites
    //-   each index, sprite in htmlWebpackPlugin.files.sprites
    //-     div.svgprite= sprite

    include /components/header/index

    main
      div
        block content

    include /components/footer/index
